<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit <%= filepath %></title>
  <style>
    body { padding: 20px; font-family: sans-serif; }
    textarea {
      width: 100%;
      height: 600px;
      font-family: monospace;
      font-size: 14px;
      padding: 10px;
      border: 1px solid #ccc;
      box-sizing: border-box;
    }
    button {
      margin-top: 10px;
      padding: 10px 20px;
      font-size: 16px;
    }
  </style>
</head>
<body>
  <h2>Edit File: <%= filepath %></h2>
  <form method="POST" action="/file/<%= filepath %>">
    <textarea name="content"><%= content %></textarea>
    <button type="submit">Save File</button>
  </form>
</body>
</html>
